import 'package:flutter/material.dart';

/// Define application colors here.
///
/// Most colors come from the Material 3 `ColorScheme`, but specific
/// brand colors, semantic colors, or static colors live here.
class AppColors {
  // Seed color for Material 3 ColorScheme — deep indigo-violet
  static const primarySeed = Color(0xFF5C6BC0);

  // Accent gradients
  static const gradientStart = Color(0xFF667EEA);
  static const gradientEnd = Color(0xFF764BA2);

  // Semantic colors
  static const success = Color(0xFF43A047);
  static const warning = Color(0xFFFFA726);
  static const info = Color(0xFF42A5F5);
  static const danger = Color(0xFFEF5350);

  // Premium
  static const premiumGold = Color(0xFFFFD700);
  static const premiumGoldDark = Color(0xFFC8A600);
}
